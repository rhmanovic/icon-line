{
  "name": "my-react-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.cjs",        // Starts the Express server
    "build": "react-scripts build",    // Builds the React app
    "serve": "serve -s build",         // Serves the build folder (optional)
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "express": "^4.18.2",              // Add Express as a dependency
    "path": "^0.12.7",                 // Add path for managing file and directory paths
    "react": "^18.2.0",                // React dependencies
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"           // The scripts for Create React App
  },
  "devDependencies": {
    "serve": "^14.0.1"                 // Optional: To serve static files without running the full Express server
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
